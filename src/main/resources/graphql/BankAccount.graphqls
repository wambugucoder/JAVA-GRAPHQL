# Bank Account Type
scalar Upload

type BankAccount {
    id:ID!,
    clientName:Client!
    currency:Currency!
    createdOn:Date
    createdAt: DateTime
    balance:Float

}

enum Currency {
    KSH,
    USD
}